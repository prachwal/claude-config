---
applyTo: "**/*.ts,**/*.tsx"
---
Skills: `ts-types` `ts-fundamentals` · `strict:true` · `interface` for shapes, `type` for unions · no `any` (use `unknown`+narrowing) · no `!` on user/API data · named exports only · prefer `satisfies` · `const` enums or string unions · after shared types: `npx tsc --noEmit`
