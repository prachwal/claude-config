---
applyTo: "**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,tests/**,e2e/**,test/**"
---
Skills: `vitest` (unit) `web-testing` (e2e) · `foo.ts`→`foo.test.ts` · `describe('Name')` + `it('does X when Y')` · test behavior not impl · `userEvent` not `fireEvent` · mock at module boundary · 1 expect/assertion · no `any` in mocks
