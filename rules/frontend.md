---
applyTo: "src/components/**,src/pages/**,src/app/**,src/features/**,src/ui/**"
---
Skills: `react` `tailwind` `wcag-aria` `web-forms` `web-data-fetching` `responsive` · PascalCase components · `use` prefix hooks · local state first · no `any` · aria-label on all interactive · Tailwind only (no inline styles) · `clsx`/`cva` for conditionals
