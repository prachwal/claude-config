---
applyTo: "src/api/**,src/server/**,src/routes/**,src/middleware/**,src/lib/**,src/db/**"
---
Skill: `.claude/skills/rest-api/SKILL.md` · Zod at boundary · no string-interpolation in DB · `{error:string}` only, no stack traces · JWTs: 15m access/7d refresh, not localStorage · verify ownership server-side
