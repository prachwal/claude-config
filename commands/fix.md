Fix the following issue: $ARGUMENTS

Protocol:
1. Read the relevant file(s) first
2. Identify the root cause (state it in one sentence)
3. Apply the minimal fix — change only what's broken
4. Verify: re-read the changed section and confirm it addresses the root cause
5. Check for similar bugs in the same file

Do NOT:
- Refactor unrelated code
- Change function signatures unless the bug requires it
- Add new dependencies
